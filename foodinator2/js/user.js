function Person(username, image, email, password) {
  this.name = username;
  this.image = image;
  this.email = email;
  this.password = password;
  this.allergien = new Array();

  this.addAllergie = function addAllergie(allergie) {
		this.participants.push(person);
	}

}